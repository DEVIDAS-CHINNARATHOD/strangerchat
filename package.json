{
  "name": "strangerchat",
  "version": "1.0.0",
  "description": "StrangerChat - Video and text chat application",
  "main": "index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "vercel-build": "cd client && CI=false npm install --no-audit --prefer-offline --no-optional && CI=false npm run build"
  },
  "dependencies": {
    "concurrently": "^8.2.0"
  }
} 