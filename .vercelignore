# Include all files by default
**

# Include the client directory and its contents
!client/
!client/**

# Include the root configuration files
!vercel.json
!package.json
!package-lock.json

# Exclude node_modules and build output
**/node_modules/
**/build/

# Exclude environment files (handled by Vercel)
.env*
!.env.example

# Exclude server files (not needed for frontend)
server/

# Exclude git files
.git/
.gitignore
